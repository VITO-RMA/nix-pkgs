diff '--color=auto' -Naur a/cmake/LZMACodec.cmake b/cmake/LZMACodec.cmake
--- a/cmake/LZMACodec.cmake	2024-10-29 00:57:03.000000000 +0100
+++ b/cmake/LZMACodec.cmake	2025-11-18 16:30:12.525180537 +0100
@@ -26,7 +26,7 @@
 
 # liblzma2
 set(LZMA_SUPPORT FALSE)
-find_package(liblzma)
+find_package(LibLZMA)
 
 option(lzma "use liblzma (required for LZMA2 compression)" ${LIBLZMA_FOUND})
 if (lzma AND LIBLZMA_FOUND)
diff '--color=auto' -Naur a/cmake/TiffConfig.cmake.in b/cmake/TiffConfig.cmake.in
--- a/cmake/TiffConfig.cmake.in	2024-10-29 00:57:03.000000000 +0100
+++ b/cmake/TiffConfig.cmake.in	2025-11-19 09:21:21.142664740 +0100
@@ -1,5 +1,66 @@
 if(NOT "@BUILD_SHARED_LIBS@")
-    # TODO: import dependencies
+    if (@ZLIB_SUPPORT@)
+        find_dependency(ZLIB REQUIRED)
+
+        find_path(Deflate_INCLUDE_DIR NAMES libdeflate.h PATH_SUFFIXES include)
+        find_library(Deflate_LIBRARY_RELEASE NAMES deflate deflatestatic PATH_SUFFIXES lib)
+
+        message(STATUS "Tiff deflate support: ${Deflate_LIBRARY_RELEASE}")
+
+        if(Deflate_LIBRARY_RELEASE AND NOT TARGET Deflate::Deflate)
+            add_library(Deflate::Deflate STATIC IMPORTED)
+            set_target_properties(Deflate::Deflate PROPERTIES
+                    INTERFACE_INCLUDE_DIRECTORIES "${Deflate_INCLUDE_DIRS}")
+
+            if(Deflate_LIBRARY_RELEASE)
+                set_property(TARGET Deflate::Deflate APPEND PROPERTY
+                        IMPORTED_CONFIGURATIONS RELEASE)
+                set_target_properties(Deflate::Deflate PROPERTIES
+                        IMPORTED_LOCATION_RELEASE "${Deflate_LIBRARY_RELEASE}")
+            endif()
+        endif()
+
+        find_path(Lerc_INCLUDE_DIR NAMES Lerc_c_api.h PATH_SUFFIXES include)
+        find_library(Lerc_LIBRARY_RELEASE NAMES LercLib Lerc PATH_SUFFIXES lib)
+
+        message(STATUS "Tiff lerc support: ${Lerc_LIBRARY_RELEASE}")
+
+        if(Lerc_LIBRARY_RELEASE AND NOT TARGET LERC::LERC)
+            add_library(LERC::LERC STATIC IMPORTED)
+            set_target_properties(LERC::LERC PROPERTIES
+                    INTERFACE_INCLUDE_DIRECTORIES "${Lerc_INCLUDE_DIRS}")
+
+            if(Lerc_LIBRARY_RELEASE)
+                set_property(TARGET LERC::LERC APPEND PROPERTY
+                        IMPORTED_CONFIGURATIONS RELEASE)
+                set_target_properties(LERC::LERC PROPERTIES
+                        IMPORTED_LOCATION_RELEASE "${Lerc_LIBRARY_RELEASE}")
+            endif()
+        endif()
+    endif ()
+    if (@JPEG_SUPPORT@)
+        find_dependency(JPEG REQUIRED)
+    endif ()
+    if (@LZMA_SUPPORT@)
+        find_dependency(LibLZMA REQUIRED)
+    endif ()
+    if (@ZSTD_SUPPORT@)
+        find_dependency(zstd CONFIG REQUIRED)
+        get_target_property(ZSTD_LIBS     zstd::libzstd INTERFACE_LINK_LIBRARIES)
+        message(STATUS "zstd link libs: ${ZSTD_LIBS}")
+        add_library(ZSTD::ZSTD ALIAS zstd::libzstd)
+    endif ()
+
+    find_library(CMath_LIBRARY NAMES m)
+    if(CMath_LIBRARY AND NOT TARGET CMath::CMath)
+        if(CMath_LIBRARIES)
+            add_library(CMath::CMath UNKNOWN IMPORTED)
+            set_target_properties(CMath::CMath PROPERTIES
+                IMPORTED_LOCATION "${CMath_LIBRARY}")
+        else()
+            add_library(CMath::CMath INTERFACE IMPORTED)
+        endif()
+    endif()
 endif()
 
 function(set_variable_from_rel_or_absolute_path var root rel_or_abs_path)
diff '--color=auto' -Naur a/libtiff/CMakeLists.txt b/libtiff/CMakeLists.txt
--- a/libtiff/CMakeLists.txt	2025-06-12 15:02:11.000000000 +0200
+++ b/libtiff/CMakeLists.txt	2025-11-18 16:33:35.273195295 +0100
@@ -168,7 +168,7 @@
   endif()
 endif()
 if(LZMA_SUPPORT)
-  target_link_libraries(tiff PRIVATE liblzma::liblzma)
+  target_link_libraries(tiff PRIVATE LibLZMA::LibLZMA)
   string(APPEND tiff_requires_private " liblzma")
 endif()
 if(ZSTD_SUPPORT)
